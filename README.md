# 窝囊费查看器 MoneyToday

「窝囊费查看器 MoneyToday」是一个原生 macOS 菜单栏小工具。它会根据你的年薪、工作时间和工作日历，实时显示“今天已经赚到多少钱”，并把这串数字翻译成更有生活感的奖励，比如一杯咖啡、一顿午餐、一只鼠标、一台相机，或者一次小旅行。

它的目标不是制造焦虑，而是给每一天的劳动一点即时反馈：打开菜单栏，看见数字在跳，也看见今天的努力已经换成了什么真实的东西。

## 设计思路

MoneyToday 围绕三个原则设计：

- **轻量**：它只住在 macOS 顶部菜单栏，不占 Dock，不打扰工作。
- **可感知**：金额以 100ms 频率刷新，日视图保留 3 位小数，周/月/年也保持持续跳动，让“正在赚钱”这件事被看见。
- **有情绪价值**：除了金额，还会展示一个具体的生活想象锚点。它不做精确价格兑换，也不告诉你“还差多少”，避免把奖励反馈变成压力。

## 1.0 功能

- 菜单栏实心钱袋图标。
- 点击图标打开悬浮面板。
- 首次启动进入设置页，填写年薪后开始计算。
- 支持设置年薪、货币、工作开始时间、工作结束时间。
- 默认工作时间为 `09:00` 到 `19:00`。
- 金额每 100ms 刷新一次。
- 今日已赚金额显示 4 位小数。
- 绿色直线进度条显示今日工作进度。
- 中国节假日和调休判断，支持本地缓存。
- 手动同步节假日数据。
- 测试模式：忽略周末和节假日，方便验收效果。
- 开机启动开关。
- 奖励文案和像素风物品图标。
- 年薪、设置和缓存都只保存在本机。

## 1.1 新增

- 产品名升级为「窝囊费查看器 MoneyToday」。
- 主界面支持 `日 / 周 / 月 / 年` 切换，周/月/年显示截至当前时刻的已赚累计。
- 日视图继续显示兑换物奖励，周/月/年不做物品折算。
- 奖励更偏向“约等于一份兑换物”，减少多份堆叠表达。
- 奖励图片改为本地精细像素风 PNG 资源。
- 接入芝麻小猫轻动效，在奖励区域陪你一起看窝囊费到账。
- 增加主界面和设置页分区隐私眼睛按钮，可把敏感金额切换为 `***`。
- 增加日期级周末自定义，可把具体周六/周日标记为工作或休息。
- 设置页增加兑换物对照表只读查看。
- 打包脚本升级为 1.1.0，并清理 `.DS_Store`、`._*` 等安装包隐藏文件。

## 1.2 新增

- UI 调整为更贴近设计稿的深色玻璃仪表风格，强化金色描边、奶油色文字和右对齐数字仪表。
- 主金额日视图改为 3 位小数，日/周/月/年都以 100ms 频率刷新。
- A/B 面和日/周/月/年切换加入平滑过渡，日视图保留奖励舞台高度，周/月/年自动收成 compact 高度，避免空白。
- 设置页改为固定弹层高度加内部滚动，A/B 面切换时弹窗高度会自动调整。
- 关闭系统矩形窗口阴影，改用 SwiftUI 异形面板阴影，避免 notch 和圆角外露灰色矩形边界。
- 资源读取兼容 `Bundle.main` 和 SwiftPM `Bundle.module`，源码运行和打包 App 都能加载芝麻动画与奖励图。
- 奖励池升级为 5 个日薪层级、10 个阶段、每阶段 6 个候选，共 300 个具体品牌商品候选。
- 300 个候选收敛到 47 张透明底像素风商品图，避免图片资产无限扩张。
- 奖励文案改为“模糊价值感 + 生活安慰”，不展示商品价格，不说“约等于/够换/已获得/档位/点亮”等任务感表达。
- 奖励区改为 1:1 左右分栏：左侧情绪文案，右侧放大的商品、完整商品名和芝麻小猫互动动画。
- 芝麻奖励动画内置 10 种姿态，每次打开窗口按商品大类选择一种姿态循环播放。

## 收入计算方式

MoneyToday 使用一个简单透明的模型：

```text
每日收入 = 年薪 / 当年工作日数量
每秒收入 = 每日收入 / 当天工作秒数
今日已赚 = 每秒收入 * 今天已经过去的工作秒数
```

如果当前不是工作日，金额不会增长；如果开启测试模式，则会忽略工作日判断，直接按设定工作时间运行。

## 情绪价值换算

MoneyToday 内置了一套面向中国大陆一线城市生活语境的人民币物价锚点表，并按日薪分为 5 档：

- `¥300 / ¥500 / ¥1000 / ¥2000 / ¥3000` 五个日薪层级。
- 每个层级分成 10 个阶段。
- 每个阶段有 6 个具体品牌商品或体验候选。
- 当前共 300 个奖励候选，并映射到 47 张可复用商品图。

奖励结果会根据日期和当前进度稳定轮换：同一天同一进度不会乱跳，第二天会有新的表达和新的奖励物品。

## 安装使用

从 Release 下载 `MoneyToday-v1.2.0.zip`，解压后打开 `MoneyToday.app`。

如果 macOS 提示来自未识别开发者，可以：

1. 右键点击 `MoneyToday.app`。
2. 选择“打开”。
3. 在系统提示里再次确认打开。

打开后，顶部菜单栏会出现 MoneyToday 钱袋图标。第一次点击图标会进入初始设置页，填写年薪并点击“保存并开始”即可。

## 从源码构建

```sh
CLANG_MODULE_CACHE_PATH="$PWD/.build/module-cache" swift build --disable-sandbox
scripts/package_app.sh
open .build/app/MoneyToday.app
```

`CLANG_MODULE_CACHE_PATH` 会把 Swift/Clang 模块缓存放到项目内，避免受系统缓存目录权限影响。`--disable-sandbox` 是为了绕开某些受限命令行环境里 SwiftPM 的二层沙箱问题。

## 验证

```sh
CLANG_MODULE_CACHE_PATH="$PWD/.build/module-cache" swift run --disable-sandbox MoneyTodayChecks
```

检查器会验证：

- 工作日前、中、后的收入计算。
- 节假日和调休逻辑。
- 测试模式。
- 节假日 API 数据解析。
- 奖励文案选择的稳定性和跨日变化。
- 300 个奖励候选、47 张透明商品图和 160 帧芝麻奖励动画资源。

## 隐私

- 年薪只保存在本机 `UserDefaults`。
- 节假日数据缓存在本机 Application Support 目录。
- MoneyToday 不上传你的年薪、设置或使用数据。

## 说明

1.2 版本暂未做 App Store 分发、正式签名和公证。它适合作为本地使用或开源体验版本。

## 项目文档

如果你想了解 MoneyToday 从想法到 1.0 发布的建设过程、产品思考和版本记录，可以阅读：

- [项目文档索引](./docs/README.md)
- [给 VCdash 读取的项目上下文摘要](./docs/vcdash-context.md)
- [建设过程记录](./docs/project-log.md)
- [1.0 发布记录](./docs/release-1.0.0.md)
- [1.1 发布记录](./docs/release-1.1.0.md)
- [1.2 发布记录](./docs/release-1.2.0.md)
- [产品思考沉淀](./docs/product-thinking.md)
